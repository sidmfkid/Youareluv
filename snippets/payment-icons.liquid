<div class="payment-types">
    <div class="container">
        <p>Safe & Secure Checkout</p>
        {% for type in shop.enabled_payment_types %}
            <img loading="lazy" height="25" width="25" src="{{ type | payment_type_img_url }}"/>
        {% endfor %}
    </div>
</div>